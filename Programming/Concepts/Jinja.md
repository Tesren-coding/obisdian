**Jinja** is a [[template engine]] for Python, commonly used with **Flask** to generate HTML pages dynamically.

Think of it this way:

- **HTML** = page structure
- **Flask** = backend logic
- **Jinja** = connects Python data to the HTML page